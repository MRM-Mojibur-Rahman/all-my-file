#include <bits/stdc++.h>
using namespace std;
int main()
{
    // int size; for array
    // cin >> size;
    // int array[size];
    // for (int i = 0; i < size; i++)
    // {
    //     cin >> array[i];
    // }
    // reverse(array,array+size);
    // for (int i = 0; i < size; i++)
    // {
    //     cout<< array[i];
    // }
    // for string;
    string s;
    cin>>s;
    reverse(s.begin(),s.end());
    cout<<s;
    
    return 0;
}